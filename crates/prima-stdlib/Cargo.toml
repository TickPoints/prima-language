[package]
name = "prima-stdlib"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
description = "Prima standard library modules (linalg, stats, io, physics, plot)"

[dependencies]
prima-runtime = { path = "../prima-runtime", version = "0.3.5" }
prima-core = { path = "../prima-core", version = "0.3.5" }
prima-syntax = { path = "../prima-syntax", version = "0.3.5" }
num-bigint = { workspace = true }
nalgebra = { workspace = true }
serde_json = { workspace = true }
